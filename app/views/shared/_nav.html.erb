<nav class="bg-green-800 w-full">
  <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-4">
    <div class="relative flex items-center justify-between h-16">
      <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-start">
        <div class="flex-shrink-0 flex items-center">
          <img class="block lg:hidden h-8 w-auto" src="https://tailwindui.com/img/logos/workflow-mark-indigo-500.svg" alt="Workflow">
          <%= link_to "PIDI-Planner", root_path, :class => 'font-semibold text-2xl tracking-tight text-white' %>
        </div>
        <% if user_signed_in? %>
          <div class="hidden sm:block sm:ml-6">
            <div class="flex space-x-2">
              <%= link_to "Dashboard", root_path, :class => 'text-gray-200 hover:text-yellow-200 p-1 rounded-md text-lg font-medium' %>
              <%= link_to "Categories", categories_path, :class => 'text-gray-200 hover:text-yellow-200 p-1 rounded-md text-lg font-medium' %>
            </div>
          </div>
        <% end %>
      </div>
      <div class="absolute inset-y-0 right-0 flex items-center pr-2 sm:static sm:inset-auto sm:ml-6 sm:pr-0">
        <p class="navbar-text float-right text-white">
          <% if user_signed_in? %>
            <%= link_to 'Edit profile', edit_user_registration_path, :class => 'text-gray-200 hover:text-yellow-200 p-1 rounded-md text-lg font-medium' %> |
            <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'text-gray-300 hover:text-red-500 p-1 rounded-md text-lg font-medium'  %>
          <% else %>
            <%= link_to "Sign up", new_user_registration_path, :class => 'text-gray-200 hover:text-yellow-200 p-1 rounded-md text-lg font-medium' %> |
            <%= link_to "Login", new_user_session_path, :class => 'text-gray-300 hover:text-red-500 p-1 rounded-md text-lg font-medium' %>
          <% end %>
        </p>
      </div>
    </div>
  </div>

</nav>
